#define WORKDIR ../
#define STYLES style\ sheets
#define STYLESHEETS --css=$(WORKDIR)$(STYLES)/columns.css --css=$(WORKDIR)$(STYLES)/colors.css --css=$(WORKDIR)$(STYLES)/fonts.css --css=$(WORKDIR)$(STYLES)/photos.css --css=$(WORKDIR)$(STYLES)/navbar.css  
